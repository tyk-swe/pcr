[package]
name = "packetcraftr-session"
description = "Bounded IP fragment and TCP stream reassembly state for PacketcraftR"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
bytes.workspace = true
serde.workspace = true
thiserror.workspace = true

# The module-level example adapts decoded layers into reassembly inputs. Neither
# crate depends on this one, so the dev-only edge introduces no cycle.
[dev-dependencies]
packetcraftr-packet.workspace = true
packetcraftr-protocol.workspace = true
