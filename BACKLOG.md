# Reviewed improvement backlog

| Baseline | Review | Scope |
|---|---|---|
| `26708188` | 11 raised → 11 kept; three independent reviews voted KEEP on every ID; no merges or split votes | All 11 findings implemented; validation results and platform limits are recorded in PLAN.md |

| Convention | Meaning |
|---|---|
| Effort | S: localized; M: multiple paths/contracts; L: cross-cutting lifecycle work |
| Impact | 3: high; 2: moderate; 1: low; qualitative estimates |
| Dependencies | Hard prerequisites only; shared-file scheduling is in PLAN.md |
| Priority | I04 addresses the highest-impact failure; I05, I07, I09 are leading small-task returns, tied with other 2/S items |

## Tier 0 — no dependencies

| ID | Title / lens | Scope | Acceptance criteria | Evidence | Effort | Impact | Deps |
|---|---|---|---|---|---|---|---|
| I04 | Preserve NDJSON deadlines / Design debt | Bound progressive failure cleanup and completion across encoder, workflow publication, and CLI termination. | Releasable blocked-writer tests cover timeout cleanup and terminal writes; a stalled-stdout subprocess exits within its configured budget plus bounded shutdown allowance. Successful streams retain contiguous sequences and exactly one terminal record; undeliverable terminals produce an incomplete-stream failure without another blocking output attempt. Preserve public APIs, authorization and budgets. | `crates/packetcraftr/src/output/stream.rs:131`; `crates/packetcraftr/src/output/stream.rs:161`; `crates/packetcraftr/src/progress.rs:178`; `crates/packetcraftr-cli/src/startup.rs:107`; `crates/packetcraftr-cli/src/commands/dns/mod.rs:132`; `crates/packetcraftr-cli/src/commands/target_workflow.rs:87` | L | 3 | [] |
| I05 | Delete TLS's diagnostic reread / Design debt | Remove the second capture analysis performed only to describe an absent stream's valid range. | One pass yields an invocation error for absent selectors, including empty captures; diagnostic wording may omit the range. Delete `count_tcp_streams` and reopening; update wording regressions and changelog. Do not expand the public, exhaustively constructible analysis Summary. | `crates/packetcraftr-cli/src/commands/tls/mod.rs:179`; `crates/packetcraftr-cli/src/commands/tls/mod.rs:235`; `crates/packetcraftr-cli/src/commands/tls/mod.rs:252`; `crates/packetcraftr-core/src/analysis/pipeline/mod.rs:125` | S | 2 | [] |
| I03 | Reject nonexistent follow selectors / Features | Check existing first-pass matched-frame evidence before successful extraction completes. | Missing TCP/UDP selectors fail with an invocation error, including empty captures; valid payload-free TCP streams and empty UDP datagrams succeed. Raw extraction emits no success payload for absent selectors; NDJSON uses its existing terminal-error contract. Update changelog. | `crates/packetcraftr-cli/src/commands/follow/mod.rs:65`; `crates/packetcraftr-core/src/analysis/follow.rs:36`; `crates/packetcraftr-cli/src/commands/tls/mod.rs:177` | S | 2 | [] |
| I02 | Export filtered normalized PCAPNG / Features | Add one explicit normalization option to `read`, using the existing frame writer. | Opt-in export contains only matching physical frames, preserves representable packet facts, and defines empty results, multiple sections/interfaces and timestamp conversion. Reject unrepresentable timestamp-less frames clearly; never invent timestamps. Preserve source-record rewriting by default, enforce input/output limits, document discarded metadata and add round-trip fixtures. | `crates/packetcraftr-cli/src/commands/read/mod.rs:67`; `crates/packetcraftr-cli/src/commands/read/mod.rs:133`; `crates/packetcraftr-core/src/analysis/pcap/writer.rs:58` | M | 2 | [] |
| I06 | Preserve piped YAML recipes / Code health | Correct stdin document-format selection using existing parsers. | Leading-comment and reordered-key YAML work equally from stdin and `.yaml` paths; JSON and packet expressions retain their behavior and malformed-input diagnostics remain useful. Preserve existing terminal rejection and byte bounds; add file/stdin parity regressions and changelog notes. | `crates/packetcraftr-cli/src/input.rs:115`; `crates/packetcraftr-cli/src/input.rs:125`; `crates/packetcraftr-cli/tests/offline_workflows.rs:853`; `examples/documents/packet-raw.yaml:1` | M | 2 | [] |
| I07 | Ship Quick Start fixtures / Code health | Include the two README-referenced fixtures in Unix and Windows release archives. | Extracted archives contain `examples/captures/tls-handshake.pcapng` and `examples/documents/packet-ipv4-udp.json`; run all three corresponding README commands from the extracted directory outside the checkout for both packaging paths. Preserve existing release checks. | `README.md:25`; `README.md:29`; `.github/workflows/release.yml:208`; `.github/workflows/release.yml:223`; `.github/workflows/release.yml:239` | S | 2 | [] |
| I08 | Remove stale ownership routes / Code health | Remove deleted CODEOWNERS targets and verify current contract/catalog precedence. | No obsolete catalog/support patterns or orphaned explanatory comment remain; check representative current paths under GitHub's last-match rule. Preserve the explicit single-owner registry exception unless history establishes a required change; use only existing owners, with no ownership framework. | `.github/CODEOWNERS:24`; `.github/CODEOWNERS:26`; `.github/CODEOWNERS:27`; `.github/CODEOWNERS:31`; `crates/packetcraftr-core/src/protocol/builtin/registry.rs:27` | S | 2 | [] |
| I09 | Apply nextest timeouts to coverage / Code health | Use the already-installed nextest runner and CI profile in llvm-cov coverage. | The coverage command selects nextest/profile ci, inherits the finite slow-test termination policy, and still writes a nonempty `lcov.info` for the existing artifact upload; no second timeout configuration. | `.github/workflows/coverage.yml:29`; `.github/workflows/coverage.yml:31`; `.config/nextest.toml:6` | S | 2 | [] |
| I10 | Bound DNS fallback test I/O / Code health | Give the loopback fallback server finite acceptance and socket I/O deadlines. | The fixture terminates if TCP fallback is never attempted, and still validates fragmented TCP responses on success; bound read/write operations and thread cleanup. Reuse the existing finite fixture pattern, without a test-server framework. | `crates/packetcraftr/src/dns/tests.rs:1472`; `crates/packetcraftr/src/dns/tests.rs:1484`; `crates/packetcraftr/src/dns/tests.rs:1502`; `crates/packetcraftr/tests/dns_tcp_contracts.rs:31` | S | 2 | [] |
| I11 | Skip discarded output conversions / Bloat | Defer follow/TLS aggregate conversion inside the existing retention helper. | When full, retention skips conversion while correctly increasing omissions; retained values, selected-session counts, text/raw/hex and NDJSON behavior stay unchanged. A conversion-count regression proves the skipped work; no separate admission abstraction. | `crates/packetcraftr-cli/src/commands/offline_analysis.rs:115`; `crates/packetcraftr-cli/src/commands/follow/rendering.rs:55`; `crates/packetcraftr/src/output/follow.rs:41`; `crates/packetcraftr-cli/src/commands/tls/rendering.rs:54`; `crates/packetcraftr/src/output/tls.rs:96` | S | 2 | [] |

## Tier 1 — depends on Tier 0

| ID | Title / lens | Scope | Acceptance criteria | Evidence | Effort | Impact | Deps |
|---|---|---|---|---|---|---|---|
| I01 | Read offline captures from stdin / Features | Support explicit `-` for `read`, `expert`, `follow`, `stats` and `tls` through the generic streaming reader. | PCAP/PCAPNG pipes match file-input results; reject terminal stdin promptly, preserve existing byte/frame/interface limits and truncation errors, and consume TLS input once. Cover empty/malformed pipes and binary rewrite. Keep live replay file-based. Document that synchronous reads can block between duration checks; do not promise interruptible reads or introduce an I/O framework. | `crates/packetcraftr-cli/src/input.rs:195`; `crates/packetcraftr-core/src/analysis/pcap/reader.rs:27`; `crates/packetcraftr-cli/src/commands/tls/mod.rs:252`; `crates/packetcraftr-cli/src/commands/replay/mod.rs:34` | M | 2 | [I05] |
