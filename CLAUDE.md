@AGENTS.md

## Agent skills

### Issue tracker

Issues and specs use local Markdown under `.scratch/<feature>/`.
For tracker operations, read `docs/agents/issue-tracker.md`.

### Triage labels

The five default triage roles are recorded as local issue statuses.
Before applying a role, read `docs/agents/triage-labels.md`.

### Domain docs

Single-context: a root `CONTEXT.md` and `docs/adr/` shared by all crates.
Before exploring, read `docs/agents/domain.md`.
